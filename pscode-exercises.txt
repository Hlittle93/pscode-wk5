// Exercise 1: Calculate the Sum of Two Numbers

Input number1 
	number1 is a
Input number2
	number2 is b 
Find the sum
	sum is a+b
Print "[sum]" 

// Exercise 2: Check if a Number is Even or Odd

Input number1
	number1 is a
	If a%2 === 0
		then Print "Even"
	Else a%2 != 0
		then Print "Odd" 

// Exercise 3: 
Write pseudo code that will perform the following.    

a) Read in 5 separate numbers.   
b) Calculate the average of the five numbers.
c) Find the smallest (minimum) and largest (maximum) of the five entered numbers.
d) Write out the results found from steps b and c with a message describing what they are

Input number1
	number1 is a 
Input number2
	number2 is b
Input number3 
	number3 is c
Input number4
	number4 is d
Input number5
	number5 is e

// To calculate average	
	Find sum 
		sum is a+b+c+d+e
	then sum/5
		Print "[sum/5]"
// To find the smallest number
	If a<b && a<c && a<d && a<e
		then print "Smallest is [a]"
	Else If b<a && b<c && b<d && b<e
		then print "Smallest is [b]"
	Else If c<b && c<a && c<d && c<e
		then print "Smallest is [c]"
	Else If d<b && d<c && d<a && d<e
		then print "Smallest is [d]"
	Else e<b && e<c && e<d && e<a
		then print "Smallest is [e]"
// To find the largest number
	If a>b && a>c && a>d && a>e
		then print "Largest is [a]"
	Else If b>a && b>c && b>d && b>e
		then print "Largest is [b]"
	Else If c>b && c>a && c>d && c>e
		then print "Largest is [c]"
	Else If d>b && d>c && d>a && d>e
		then print "Largest is [d]"
	Else e>b && e>c && e>d && e>a
		then print "Largest is [e]"

-- END --